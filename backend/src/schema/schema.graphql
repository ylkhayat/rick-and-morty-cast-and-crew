type Query {
  me: User
  bookmarks: [Bookmark!]!
}

type Mutation {
  loginOrSignup(username: String!): AuthPayload!
  createBookmark(url: String!, description: String!): Bookmark!
}

schema {
  query: Query
  mutation: Mutation
}
